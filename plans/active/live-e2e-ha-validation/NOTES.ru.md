2026-04-27T20:01:39Z
- Baseline: rollout healthy; legacy ai-models.deckhouse.io CRDs removed; stale DMCR maintenance ack leases removed.
- HF: Model ai-models-e2e/hf-tiny-llama reached Ready; worker logs show metadata, direct object-source, direct-upload sealing, manifest publish and inspect; workload delivery with MaterializeBridge succeeded.
- Pre-change reviews: integration_architect and backend_integrator were used before destructive HA actions. Guardrails: capture DMCR direct-upload verification policy; preflight upload ingress/TLS/auth; do not overlap DMCR restart with delete/GC; HA must inspect state secret continuity and PublicationResumed rather than only eventual Ready.
- Upload: external ingress/TLS/auth preflight succeeded; multipart direct upload staged and publish worker completed, but Model ai-models-e2e/upload-tiny-gguf flipped to Failed with "source worker upload source requires a staged upload handle".
- Root cause: catalogstatus wrote backend cleanup handle and requeued before Ready status for upload-source success; the next reconcile saw status=Publishing with backend cleanup handle and no staged upload handoff.
- Local fix: keep staged upload handoff requeue semantics, but project successful backend publication status in the same reconcile after backend cleanup handle is stored; added regression coverage for the missing staged-handle replay window.
- HA hardening: sourceworker now observes an existing non-stale retained worker before building a new source plan, so a succeeded upload worker can still be consumed after a rare status-write failure that already replaced cleanup state with the backend handle.
- Upload UX fix: replaced stale public "worker pod" upload-session messages with session-state wording.
- HA status: controlled interruption replay is intentionally not completed on the current live image because upload publication is already deterministically broken there. Next executable slice is post-fix HF/Upload HA replay after the new build is deployed.
- Delete/GC: e2e workload and both Models deleted; runtime cleanup-state/upload-session/sourceworker secrets disappeared; `dmcr-gc-56f1d3e2-*` and `dmcr-gc-7ecdc214-*` requests are queued for the protected DMCR maintenance window.
- GC evidence: both request secrets reached `done` at 2026-04-27T20:21:55Z; maintenance ack quorum was 2/2; registry GC deleted 7 blobs.
- Cleanup: namespace `ai-models-e2e` deleted after GC evidence was captured.
- GC UX fix: added a structured `deletedRegistryBlobCount` result/log field so operators do not have to parse `registryOutput` for physical deletion evidence.
- RBAC smoke: live user-authz/rbacv2 ClusterRoles only grant `models`, `clustermodels` and `moduleconfigs` rules; static deny-path scan found no `secrets`, `pods/log`, `pods/exec`, `pods/attach`, `pods/portforward`, `*/status` or `*/finalizers` in user-facing roles. Controller SA can manage its internal runtime secrets/status; DMCR SA cannot read Models or pods.
- Final review: reviewer found no scoped code regression; remaining risks are incomplete post-fix HA replay and the structured GC deleted-blob counter depending on registry output text.
- Verification: narrow upload/catalogstatus/publishobserve/dmcr-gc tests, full `go test ./...` in images/controller and images/dmcr, `git diff --check`, and `make verify` passed.
- 2026-04-27T23:39:27+03:00 ClusterModel/Gemma 4 preflight: `google/gemma-4-E2B-it` is public/non-gated by HuggingFace model API and `config.json` resolves unauthenticated, so the live test can use a cluster-scoped `ClusterModel` without namespace-local credentials.
- 2026-04-27T23:43:46+03:00 ClusterModel/Gemma 4 live test started on `k8s.apiac.ru`: `gemma-4-e2b-it-e2e`, source `https://huggingface.co/google/gemma-4-E2B-it`, resolved revision `b4a601102c3d45e2b7b50e2057a6d5ec8ed4adcf`, publish size `10278821752` bytes.
- Controlled interruption evidence before defect: deleting one controller pod and one DMCR pod during upload kept publication progressing and both Deployments returned to 2/2 with zero restarts.
- Defect found: deleting active source-worker pod during direct upload marked the `ClusterModel` terminal `Failed: context canceled`, while `ai-model-publish-state-92c56a57-b6ac-46c9-8e7b-563405b27b17` still had `phase=Running`, one completed layer and current raw-layer session progress. This is a false terminal failure for a replayable runtime interruption.
- Local fix: `publishobserve.ObserveSourceWorker` now classifies `context canceled` / `signal: terminated` failed worker results as interrupted runtime, keeps public status `Publishing`, deletes the failed runtime pod and lets the next reconcile recreate/resume from saved direct-upload state.
- Cleanup defect found: deleting the failed `ClusterModel` removed publication runtime state without creating a targeted DMCR GC request, so the current direct-upload session would rely on age-bounded orphan cleanup instead of immediate request-scoped cleanup.
- Local fix: delete flow now detects an in-flight direct-upload session when no backend cleanup handle exists, snapshots the session token into a DMCR GC request before deleting runtime state, then continues finalizer cleanup.
- Cleanup after failed live run: `gemma-4-e2b-it-e2e`, owner-labelled runtime resources and `ai-models-e2e` namespace were removed; live ai-models controller/DMCR Deployments returned to 2/2 with zero container restarts.
- Verification: `go test ./internal/application/publishobserve ./internal/application/deletion ./internal/controllers/catalogstatus ./internal/controllers/catalogcleanup`, full `go test ./...` in `images/controller`, full `go test ./...` in `images/dmcr`, `git diff --check` and `make verify` passed.
- 2026-04-28T08:29:54+03:00 Runbook refreshed after storage accounting, Diffusers/video capability, SharedDirect readiness, upload-gateway identity, desired-artifacts and system placement slices. No live destructive actions were executed during this refresh. Next command from user should start the prepared comprehensive e2e/HA run.
- 2026-04-28T09:40:00+03:00 Live smoke was started with `Model/ai-models-e2e/hf-direct-tiny-phi`; it reached `Ready` and emitted `PublicationSucceeded`, then was stopped before destructive HA steps because upload Ingress TLS wiring needed a template fix. The model and namespace-scoped runtime resources were removed; `kubectl get model,pod,job,secret,pvc -n ai-models-e2e` returned no resources.
- 2026-04-28T10:10:00+03:00 Live alert cleanup: legacy backend PrometheusRule was removed in `k8s.apiac.ru`, false controller/DMCR `TargetAbsent` rules were removed from live PrometheusRules, and all `ai-models` alert objects moved out of `Firing`. Historical `ClusterObservabilityAlert` records remain read-only in `Resolved`.
- 2026-04-28T10:20:00+03:00 RBAC runbook tightened after comparison with virtualization `user-authz-cluster-roles.yaml`: `ai-models` follows the same access-level delta-fragment pattern. Full e2e must record `can-i` allow/deny rows for `User`, `PrivilegedUser`, `Editor`, `Admin`, `ClusterEditor`, `ClusterAdmin`, global `SuperAdmin`, plus rbacv2 `use`/`manage`; sensitive human-facing paths remain explicit deny gates.
- 2026-04-28T10:30:00+03:00 RBAC static guardrails tightened after Deckhouse `user-authz` hook review: custom module fragments are checked for six supported access levels, module-local `SuperAdmin` fragment is rejected as misleading, rbacv2 `use`/`manage` names/labels/resources/verbs are checked in render validation, and live e2e still must execute the full effective `can-i` matrix including global `SuperAdmin`.
- 2026-04-28T11:13:35+03:00 E2E plan refreshed after RBAC/observability hardening. Next run must first prove rollout/placement/TLS/alerts, then live collector health metrics (`catalog-state`, `runtime-health`, `storage-usage`), normalized `err` structured logs, static RBAC render tests, full `can-i` matrix, HF Direct/Mirror, Upload, Diffusers image/video, storage reservation, workload delivery, controlled interruptions and delete/GC evidence.
- 2026-04-28T11:54:36+03:00 Live preflight on `k8s.apiac.ru`: `Module/ai-models` Ready; controller/upload-gateway/DMCR Deployments are 2/2, pods are placed on worker nodes (`k8s-w2`, `k8s-w5`, `k8s-w6`) with zero restarts after rollout.
- Metrics scrape defect found before destructive e2e: controller and DMCR `kube-rbac-proxy` logs showed `tokenreviews.authentication.k8s.io is forbidden` for service accounts `d8-ai-models:ai-models-controller` and `d8-ai-models:dmcr`; after granting review rights, proxy logs showed `Forbidden ... d8-monitoring:prometheus ... deployments/prometheus-metrics`, proving missing `rbac-to-us` scrape access too.
- Local/live fix: added dedicated `*-metrics-auth` ClusterRole/Binding for controller and DMCR proxy authn/authz, plus `access-to-ai-models-prometheus-metrics` Role/Binding for `d8-monitoring:prometheus` and `d8-monitoring:scraper`, matching Deckhouse/virtualization `rbac-for-us` + `rbac-to-us` metrics pattern. Live can-i checks now return `yes` for proxy `TokenReview`/`SubjectAccessReview` and Prometheus/scraper `get deployment/<controller|dmcr>/prometheus-metrics`.
- Evidence after live RBAC fix: controller/DMCR `kube-rbac-proxy --since=1m` produced no new errors; `D8AIModelsControllerTargetDown` and `D8AIModelsDMCRTargetDown` moved to `Resolved`. Remaining firing alert is unrelated `D8GPUControlPlaneControllerTargetAbsent`.
- Verification for template fix: `python3 -m unittest tools/helm-tests/validate_renders_test.py`, `make helm-template`, `make kubeconform`, `git diff --check`, and `git diff --cached --check` passed.
- Live hotfix ownership hardening: temporary RBAC objects created for immediate scrape recovery were annotated/labeled with Helm ownership (`meta.helm.sh/release-name=ai-models`, `meta.helm.sh/release-namespace=d8-system`, `app.kubernetes.io/managed-by=Helm`) so the next ModuleRun can adopt/update them instead of failing on existing unmanaged resources.
- 2026-04-28T18:11:32+03:00 Large-model HF Direct e2e run started on `k8s.apiac.ru` with run id `20260428181132`: Gemma 4 E2B ClusterModel plus Qwen 2.5 1.5B tool-calling, BGE large embedding, BGE reranker large, Whisper large v3 turbo, MMS TTS, Qwen VL 3B, DreamShaper 8 and Zeroscope 576w Models.
- Controlled interruptions during the run: deleted active Qwen source-worker pod, one controller pod and one DMCR pod. Controller and DMCR returned to 2/2 with zero container restarts; Qwen resumed and reached Ready.
- Live status after the run: Gemma 4 E2B, Qwen tool-calling, BGE embedding, BGE reranker, Whisper, MMS TTS and Qwen VL reached Ready. Zeroscope and DreamShaper failed.
- Defect found: current live Diffusers validator rejects valid component `.bin` weights, so Zeroscope failed early with `Diffusers requires at least one .safetensors file`. Local fix: Diffusers validation/profile/source summaries now accept component-scoped `pytorch_model*.bin`, `model.bin` and `diffusion_pytorch_model*.bin` without treating root LLM `.bin` as Diffusers.
- Defect found: DreamShaper failed after DMCR scheduled GC ran while publication was still active. DMCR GC deleted registry blobs for an in-flight direct-upload artifact and manifest push returned `MANIFEST_BLOB_UNKNOWN`. Local fix: DMCR GC now defers queued requests and requeues armed requests while any `ai-models-publication` Pod is Pending/Running/Unknown, before setting the maintenance gate or running registry GC.
- Defect found: the same DreamShaper retry path later hit `direct upload uploaded size ... exceeds planned size ...` by exactly one part size. Local fix: direct-upload state normalization drops stale `currentLayer` progress only when completed layers already exactly match the persisted plan.
- Defect found: BGE reranker live status was classified as `Embeddings` because HF `pipeline_tag=feature-extraction` is generic, while `model-index.results.task.type=Reranking` is specific. Local fix: HuggingFace profile resolution prefers an unambiguous normalized model-index task over generic pipeline tag.
- Defect found: workload-delivery touched module namespace internal `Deployment/ai-models-controller` and emitted `ModelDeliveryRemoved`. Local fix: workload-delivery predicates and reconcilers ignore the module namespace.
- Defect found: live runtime logs still used old image and duplicate structured keys. Local fix already present: controller/runtime and DMCR logging now use stable `component` and normalized `err` without duplicate `logger`/`error` keys.
- Verification for local fixes: narrow controller/DMCR package tests passed first; then full `cd images/controller && go test ./...`, full `cd images/dmcr && go test ./...`, and `git diff --check` passed.
- 2026-04-28T18:49:20+03:00 User reported controller/DMCR pod restarts. Live check showed current `ai-models-controller`, `dmcr` and `upload-gateway` Pods have container `restartCount=0`; 90-second monitoring kept all restart counts at zero. The visible `Killing` events were caused by the controlled HA interruption test. Separate real defect remains: old live `workloaddelivery` touched `Deployment/ai-models-controller` (`ModelDeliveryRemoved` x11), which can look like self-restarts if it patches the Pod template; the local module-namespace guard is the required fix and needs rollout.
- Cleanup after the noisy large-model run: deleted all temporary `ai-models-e2e` Models and `ClusterModel/e2e-gemma4-e2b-20260428181132`; no active publication Pods remained, and the module Deployments still had zero restarts after cleanup. Per-model DMCR GC requests are queued for the normal protected window.
