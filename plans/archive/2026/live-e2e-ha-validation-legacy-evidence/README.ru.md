# Legacy live e2e evidence

Этот архив хранит live evidence `k8s-apiac-20260429-001154`, собранный до
перехода workload delivery на names-only `SharedDirect` contract.

Данные оставлены только как historical record. Они больше не являются
исполняемой матрицей для `plans/active/live-e2e-ha-validation`, потому что
содержат legacy `model-refs`, `MaterializeBridge` и `AI_MODELS_MODEL_*`
runtime env.
