{{- if and (include "ai-models.isEnabled" .) (include "ai-models.hasDiscoveredDexCA" .) }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ai-models.backendOIDCCASecretName" . }}
  namespace: {{ include "ai-models.namespace" . }}
  {{- include "helm_lib_module_labels" (list . (dict "app" "ai-models" "component" "backend")) | nindent 2 }}
type: Opaque
data:
  ca.crt: {{ include "ai-models.discoveredDexCA" . | b64enc | quote }}
{{- end }}
