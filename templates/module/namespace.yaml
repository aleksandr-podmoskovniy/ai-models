---
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "ai-models.namespace" . }}
  labels:
    heritage: deckhouse
    module: {{ include "ai-models.moduleName" . }}
{{ include "helm_lib_kube_rbac_proxy_ca_certificate" (list . (include "ai-models.namespace" .)) }}
